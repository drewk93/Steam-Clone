/* 
Calculate rating metric whether review is overwhelmingly positive,
very positive, mixed, negative, etc. Based off the ratio of positive to negative reviews.

*/

const recommmendRatio = (TotalReviewsRating) => {

    // take in an array of the total number of review ratings, sum up each negative vs positive
    let positiveReviewsSum = 0;
    let negativeReviewsSum = 0;

    let ratioResult = ''



    return ratioResult;
}